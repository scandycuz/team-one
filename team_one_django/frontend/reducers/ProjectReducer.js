const nullState = {
  project: {
    points: 25,
    maxPoints: 100,
  }
}

const ProjectReducer = (state = nullState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default ProjectReducer;
